run:
  timeout: 2m

linters:
  enable:
    - errname
    - exportloopref
    - goconst
    - godot
    - goimports
    - unconvert
    - whitespace

linters-settings:
  errcheck:
    exclude-functions:
      - (net/http.ResponseWriter).Write
  goconst:
    min-len: 2
    min-occurrences: 2
